# Sandbox docs

Links work in any file type: the default-include rule (7) matches vscl-homepage here too.

NOTE(from-extended-config) — this link comes from shared.config.ts via `extends`.

## string-jump

The docs marker in test.ts jumps here via a plain-string jumpPattern.

## Regex jump target

The abs marker in test.ts jumps here via a RegExp jumpPattern.
