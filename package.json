{
  "name": "vsc-links",
  "displayName": "VSC Links",
  "description": "Create any links to files and websites based on patterns.",
  "author": "Samuel Braun <sam@webry.com>",
  "publisher": "webry",
  "license": "GPL-3.0-only",
  "version": "2.4.1",
  "icon": "icon.png",
  "homepage": "https://github.com/webry-com/vsc-links#readme",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "bugs": {
    "email": "sam@webry.com",
    "url": "https://github.com/webry-com/vsc-links/issues"
  },
  "keywords": [
    "links",
    "tool",
    "url"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/webry-com/vsc-links"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/main.js",
  "contributes": {
    "commands": [
      {
        "command": "vsc-links.createConfig",
        "title": "VSC Links: Create Config File"
      },
      {
        "command": "vsc-links.restartVSCodeLinks",
        "title": "VSC Links: Restart"
      },
      {
        "command": "vsc-links.refreshVSCodeLinksProviders",
        "title": "VSC Links: Refresh Config"
      }
    ]
  },
  "capabilities": {
    "virtualWorkspaces": {
      "supported": false,
      "description": "Using VSCode Links is not possible in virtual workspaces."
    },
    "untrustedWorkspaces": {
      "supported": false,
      "description": "VSCode Links requires workspace trust because it loads configuration files and code from the workspace and executes it."
    }
  },
  "extensionKind": [
    "workspace"
  ],
  "scripts": {
    "watch": "tsc -watch -p ./",
    "check": "tsc -p ./",
    "dev": "esbuild ./src/extension.ts --bundle --outfile=out/main.js --external:vscode --format=cjs --platform=node --sourcemap",
    "vscode:prepublish": "npm run compile",
    "compile": "esbuild ./src/extension.ts --bundle --outfile=out/main.js --external:vscode --format=cjs --platform=node --minify",
    "test": "vscode-test",
    "pretest": "npm run compile",
    "prepack": "vsce package --pre-release",
    "prepubl": "vsce publish --pre-release",
    "rel": "vsce publish"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "18.x",
    "@types/uuid": "^10.0.0",
    "@types/vscode": "^1.90.0",
    "@vscode/test-cli": "^0.0.8",
    "@vscode/test-electron": "^2.3.9",
    "esbuild": "^0.25.8",
    "globals": "^15.6.0",
    "prettier": "^3.3.2",
    "typescript": "^7.0.2",
    "vscl": "^2.3.0"
  },
  "dependencies": {
    "c12": "^1.11.1",
    "minimatch": "^10.0.1",
    "uuid": "^10.0.0",
    "zod": "^3.23.8"
  }
}